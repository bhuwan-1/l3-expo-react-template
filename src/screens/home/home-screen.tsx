import Home from '@template/components/home/home';

// The screens are the main screens of the app.
// These are to import the core home component which has other components inside it.
export default function HomeScreen() {
  return <Home />;
}
